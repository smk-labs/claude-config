- **The map of every project is `~/Projects/MAP.md`**, in the private `SMKeramati/daftar` repo cloned at `~/Projects`. One line per project: path, remote, one sentence. Look one up with `./daftar find <word>`, and give a new project its line the same session.
- **Every secret comes from Bitwarden**, never from a file, a backup or git history: `bw list items --search <word>`, then `bw get item <id>`. Item names are the paths they had under `open/`, e.g. `accounts/vercel.env`. Never print a value into chat.
